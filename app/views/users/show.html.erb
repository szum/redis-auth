<div class="row">
  <div class="offset-4 col-md-4">
      <h1 class="mt-4 text-center">🎉</h1>
      <h3 class="text-center">
        <ul class="list-group">
          <% @user.each do |field| %>
            <li class="list-group-item"><%= field =%></li>
          <% end %>
        </ul>
      </h3>
      <% if logged_in? %>
        <div class="col text-center">
          <%= link_to "Log out", logout_path, class: "mt-4 btn btn-primary", method: :delete %>
        </div>
      <% end %>
  </div>
</div>
